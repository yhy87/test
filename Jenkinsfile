echo "---build start---"

stage('Checkout Stage') {
		echo "---Checkout---"
		echo "${P_VERSION}"
		sh "pip3 install requests"
}

stage('Build Stage') {
		echo "---Build Stage---"
}

stage('Push Stage') {
		echo "---Push Stage---"
}
